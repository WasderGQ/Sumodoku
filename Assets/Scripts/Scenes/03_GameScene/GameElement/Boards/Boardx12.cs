using UnityEngine;

namespace WasderGQ.Sudoku.Scenes.GameScene.GameElement.Boards
{
    public class Boardx12 : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
